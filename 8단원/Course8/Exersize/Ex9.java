package Course8.Exersize;

public class Ex9 {
    //실수의 2차원 행렬을 나타내는 Java 클래스 Matrix를 구현합니다. 이 클래스는 다음 메서드를 내보내야 합니다:
    //Matrix(int n, int m) : 모든 값이 처음에 0으로 설정된 nxm 크기의 행렬을 생성하는 생성자;
    //void save(String 파일명) : 행렬의 내용을 파일명으로 지정된 파일에 저장합니다;
    //static Matrix read(String filename) : 파일명으로 지정된 파일로부터 행렬에 관한 데이터를 읽어, 행렬을 생성하고 이를 리턴합니다;
    //Matrix 합계(Matrix m) : 두 행렬의 치수가 같은 경우, 객체와 m의 합계인 행렬을 리턴하고, 그렇지 않은 경우는 null을 리턴합니다;
    //행렬 곱(행렬 m) : 두 행렬의 차원이 호환되는 경우 객체와 m의 곱인 행렬을 반환하고, 그렇지 않으면 null을 반환합니다.
}
